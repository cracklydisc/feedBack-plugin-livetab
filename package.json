{
  "name": "feedback-plugin-livetab",
  "version": "0.37.0",
  "private": true,
  "description": "Tests for the Live Tab visualization plugin for fee[dB]ack.",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
